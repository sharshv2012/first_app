import 'package:flutter/material.dart';
void main(){
  runApp(const MaterialApp(home: Scaffold(body: Center(child: Text("hiii"))))); //using cosnt is used to avoid duplicate memory allocation if the widgets are reused it's a clean coding practice in flutter.

} 
  // scaffolds sets up the screen , it's inside the materialApp





